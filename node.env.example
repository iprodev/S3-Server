# S3 Node Environment Configuration
# Place in /etc/s3_server/node.env

# Authentication token
NODE_AUTH_TOKEN=changeme-use-a-strong-token

# Optional: Override defaults
# LISTEN_ADDR=:8080
# DATA_DIR=/var/lib/s3_server/data
# LOG_LEVEL=info
